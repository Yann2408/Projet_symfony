<form method="post" action="{{ path('comment_delete', {'id': comment.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer le commentaire?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btnDeleteCommentaire">Supprimer le commentaire</button>
</form>
