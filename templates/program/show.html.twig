{% extends "base.html.twig" %}

{% block title %}{{seasons.0.program.title}}{% endblock %}

{% block body %}

<img src="{{seasons.0.program.poster}}">
<h1>{{seasons.0.program.title }}</h1>

     {% for season in seasons %}
        <div>
            <a href="{{ path("program_season_show", { programId: season.program.id, seasonId: season.id}) }}">Saison numéro:{{season.number}}</a>
        </div>
    {% endfor %}


    {# <div class="media">
        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>
            <p>{{ program.summary }}</p>
            <p>Catégorie : {{ program.category.name }}</p>
        </div>
    </div> #}

    <a href="{{ path('program_index') }}"> Retour à la liste des programmes</a>

{% endblock %}