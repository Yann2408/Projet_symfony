<form class="formDelete"S method="post" action="{{ path('program_delete', {'id': program.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer la série?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program.id) }}">
    <button class="btnDelete">Supprimer la série</button>
</form>
