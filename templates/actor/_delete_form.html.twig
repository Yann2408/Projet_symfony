<form method="post" action="{{ path('actor_delete', {'id': actor.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer l\'acteur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.id) }}">
    <button class="btnDelete">Supprimer l'acteur</button>
</form>
